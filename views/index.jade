extends layout

block content
  h1= title
  p Welcome to #{title}
    h1 Breck
      h2 #{breck.sunset}
      h2 #{breck.sunrise}
      h2 #{breck.bomMaxTempF}
      h2 #{breck.bomMinTempF}
      h2 #{breck.topMaxTempF}
      h2 #{breck.topMinTempF}
    h1 Vail
      h2 #{vail.sunset}
      h2 #{vail.sunrise}
      h2 #{vail.bomMaxTempF}
      h2 #{vail.bomMinTempF}
      h2 #{vail.topMaxTempF}
      h2 #{vail.topMinTempF}
    h1 Beaver Creek
      h2 #{bc.sunset}
      h2 #{bc.sunrise}
      h2 #{bc.bomMaxTempF}
      h2 #{bc.bomMinTempF}
      h2 #{bc.topMaxTempF}
      h2 #{bc.topMinTempF}
    h1 Keystone
      h2 #{ks.sunset}
      h2 #{ks.sunrise}
      h2 #{ks.bomMaxTempF}
      h2 #{ks.bomMinTempF}
      h2 #{ks.topMaxTempF}
      h2 #{ks.topMinTempF}
    h1 Araphahoe Basin
      h2 #{ab.sunset}
      h2 #{ab.sunrise}
      h2 #{ab.bomMaxTempF}
      h2 #{ab.bomMinTempF}
      h2 #{ab.topMaxTempF}
      h2 #{ab.topMinTempF}
